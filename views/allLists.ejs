<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%- include("partials/style")%>
    <title>Listas</title>
</head>

<body>
    <div id="header" class="h1 d-flex justify-content-center align-items-center">
        Listas
    </div>
    <div class="container">
        <form id="add" method="get" action="/createlist">
            <input type="submit" id="addButton" class="btn" value="Nova Lista"></input>
        </form>
        <div class="d-flex flex-column justify-content-center">
            <%for(i=0; i<lists.length;i++){%>
                <div class="lista card">
                    <div class="card-body d-flex justify-content-between align-items-center">
                        <a href="/list/<%=lists[i].id_list%>"><%=lists[i].nome%></a>
                        <div class="d-flex justify-content-center"> 
                            <form method="get" action="/editlist/<%=lists[i].id_list%>">
                                <button type="submit" class="btn btn-info m-1">
                                    <span class="material-symbols-outlined d-flex">
                                        edit
                                    </span>
                                </button>
                            </form>
                            <form  method="POST" action="/deletelist/<%=lists[i].id_list%>?_method=DELETE">
                                <button type="submit" class="btn btn-danger m-1">
                                    <span class="material-symbols-outlined d-flex">
                                        delete
                                    </span>
                                </button>
                            </form>
                        </div>
                    </div>

                </div>
            <%}%>
        </div>
    </div>

</body>

</html>
